# For Vite projects, only variables that start with the prefix "VITE_" to client-side code as a security measure to prevent accidentally leaking server secrets.

# Render's env var setting overrides the variable below — but it's good practice to keep the file accurate in the repo for documentation purposes.
VITE_API_URL=https://react-dotnet-website.onrender.com